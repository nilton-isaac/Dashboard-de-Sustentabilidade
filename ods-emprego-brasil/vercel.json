{
  "buildCommand": "npm install -g @angular/cli && npm run build",
  "outputDirectory": "dist/ods-emprego-brasil/browser",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}